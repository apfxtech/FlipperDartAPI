name: flipperlib
description: Flipper Zero BLE/USB transport and RPC client library.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  fixnum: ^1.1.1
  flutter_libserialport: ^0.6.0
  permission_handler: ^11.3.1
  protobuf: ^6.0.0
  universal_ble: ^1.0.0
  usb_serial: ^0.5.2

dev_dependencies:
  flutter_lints: ^6.0.0
